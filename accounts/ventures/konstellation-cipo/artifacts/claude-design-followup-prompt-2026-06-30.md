Refine the Shadow CIPO site you just designed. The site has exactly ONE job: get a venture-backed founder to request a Competitor Filing Teardown (a 4-field opt-in: Name, Work email, Company, top competitor). The brand one-liner is "Shadow CIPO — your behind-the-scenes Chief IP Officer." Every change below should make that opt-in more likely and the site read like a top-tier boutique advisory built it, not a template. Your first pass satisfied the spec but landed generic and visually flat (the failure mode we're fixing: no real hero, unstyled stacked text, "1/2/3" steps as tiny artifacts, flat full-width bands, no reading measure, plain tiers/quote/Q&A). Keep all copy editable and token-driven. Do NOT regress any §2 ship-blocker.

Apply these refinements in priority order.

1. REAL HERO + VISUAL HIERARCHY (highest priority — the biggest miss in the failed build)
- Build a composed above-the-fold hero that reads as ONE unit, not a stack of centered text. Establish a single dominant focal point: the promise headline in editorial serif (Source Serif 4 or EB Garamond), 44–56px desktop, tight tracking, tight leading (~1.05–1.1), max ~16 words on two or three deliberate lines.
- Lock an explicit subordinate hierarchy beneath it: mono eyebrow (12px uppercase tracked, "For venture-backed founders raising or preparing to raise") → dominant serif promise → Inter subhead (~19px, muted) → exactly ONE primary CTA button (brass) → a quiet proof strip. Nothing else competes above the fold; no secondary button styled like the primary.
- Use the brand-forward H1: "Your Shadow CIPO: continuous IP intelligence and a Chief IP Officer's judgment — at a subscription, not by the hour." Keep the gloss "your behind-the-scenes Chief IP Officer" as a deliberate paired lockup under the wordmark, never a tacked-on line.
- Left-align or use an asymmetric grid for the hero. No centered-everything. No generic-SaaS blue, no pill-shaped gradient buttons, no icon trio.

2. THE SHADOW-HERO CONCEPT (the ownable visual idea — commit to it tastefully)
- Render the brand idea "founder in the light, the capable operator in the background" as a confident DARK hero (deep ink, e.g. #0B1220) that resolves into clean, warm-paper light reading sections below. Make the dark→light transition intentional (a clean tonal step or one soft rule), reinforcing depth and authority.
- This is light/shadow as contrast and leverage, NEVER noir or surveillance. No shadowy figures, blacked-out silhouettes, hooded/spy/cloak/dark-web imagery. Explicitly: the hero is NOT a starfield and carries no astronomy motif of any kind.
- On the dark ground, verify the serif headline, subhead, brass CTA, and proof strip all clear WCAG AA contrast; report the computed brass-on-dark and on-accent-text ratios as numbers, not "passes."

3. REUSABLE HERO + LEAD-MAGNET + CAPTURE BLOCK (don't regress; make it swappable)
- Build hero + lead-magnet + capture as ONE self-contained, parameterized block, not a one-off keyed to the teardown. The eyebrow, promise/H1, subhead, CTA label, capture-form heading, helper line, and the 4 field labels must all be data/token-driven inputs to the block.
- Render BOTH the Home closing band and the /competitor-teardown page from this same block (proves reuse); keep the field set and success state identical across instances. Include a short list of the swappable slots so a future campaign page (NIH/TABA, IP-Velocity-Score) spins up by swapping the magnet, not redesigning.

4. CTA REPEATED AT EVERY SCROLL DEPTH + LOW-FRICTION CAPTURE
- Place the primary CTA in at least three positions with one identical identity (same label, accent, weight): hero, a mid-page band before the tiers, and the closing capture band. Add one always-reachable path on long scroll — a sticky nav "Request a teardown" button on desktop OR a sticky bottom CTA bar on mobile. No dead zone longer than ~1.5 viewports without a visible way to convert.
- Capture form: single column, full-width stacked inputs, exactly 4 required fields (Name, Work email, Company, "Your top competitor (or the space you're worried about)"). Persistent labels above inputs, not placeholder-only. One full-width primary submit, "Send me the teardown," the heaviest element in the band. Keep the ~18-month publication-lag helper line as quiet supporting text below the fields, never a wall above them. Design an explicit in-place success state ("We'll research and send your teardown.") as a second frame — no redirect. Make it read as "answer 4 quick things," not "fill out a form."
- Pull the proof strip ("Led by Will Rosellini" · "15 years in IP monetization" · "Raised a Series A himself") into the same viewport as the hero CTA, styled as scannable credential chips (mono labels, editorial weight). "Raised a Series A himself" is the trust unlock for this skeptical audience — give it visible weight. Keep the logo-absence line as a confident aside near the proof, not a defensive standalone band.

5. THE 3-STEP / TIERS / PULL-QUOTE / Q&A TREATMENTS (fix each decisively — these all rendered as flat artifacts)
- How it works: each step is a designed editorial unit — a large display numeral (48–72px, brass or muted ink, a true design element), a serif step title, one Inter line at measure. Three columns with generous gutters on desktop, stacked on mobile, aligned to a baseline grid with a thin connecting rule so 1→2→3 reads as progression. No numbered chips, no lucide-icon trio.
- Tiers (Scout / Shield / Arsenal): three equal-weight cards, subtle border or single-layer shadow, generous interior padding, white surface on warm paper. Internal hierarchy: mono tier label → serif name → Inter description → the no-pricing line as a deliberate mono/small-caps element ("Pricing set on the call" / "Pricing shared on the call"), never an empty slot, dash, "TBD," literal "[pricing pending]" brackets, or a greyed placeholder that looks broken. Add one confident line owning the no-price choice as bespoke/senior positioning ("priced to your engagement"). Tiers must escalate visibly Scout→Shield→Arsenal, but the only action out of the section is the teardown CTA — no per-tier buy button competing with the opt-in. Optionally one brass accent on a single card for progression, never a "most popular" pricing tactic.
- Founder pull-quote: a real magazine pull-quote — large serif, set narrower than full width, generous whitespace, ONE restraint device (oversized opening quote mark OR a brass left rule OR a generous indent — pick one, don't stack). Attribution in small mono/muted beneath. A deliberate pause in the page rhythm, not a gray card.
- Honest answers: an open, legible editorial Q&A — questions AND answers visible, no accordion/expander gimmick. Question in serif or mono-emphasis, answer in body at measure, hairline separators, strong vertical rhythm. Confident and a little blunt, matching the tone. Keep the "strategy and judgment, not legal representation" answer intact here.

6. TYPOGRAPHY, RHYTHM, COLOR, MOTION (the editorial-vs-generic levers)
- Lock the three-role type system and make the roles visibly distinct: editorial serif (Source Serif 4 / EB Garamond, not Playfair) for display; Inter 400–600 for body/UI; mono (IBM Plex Mono / JetBrains Mono) ONLY for eyebrows, labels, step numerals, tags, and the no-pricing line. Mono is the recurring "precision/analysis" signature — sparing, never body. Scale: hero 44–56 / H2 28–32 / body 17–19 / labels 12px mono uppercase tracked, with per-role leading and letter-spacing.
- Set a reading measure of ~68–72ch on ALL prose (pull-quote, how-it-works lines, Honest answers, body). Nothing edge-to-edge. This is the single highest-leverage editorial lever — verify it everywhere.
- Kill the flat full-width bands: real vertical rhythm with generous, consistent section padding (96–128px desktop), a shared content max-width / grid spine everything aligns to, mono section eyebrows anchoring each section as composed chapters. Alternate paper (#FAF8F3, pick one and commit) / white SPARINGLY and only to separate genuinely distinct sections — never faint stripes for their own sake.
- One accent only: brass #A16207 for CTAs, key rules, and emphasis, used sparingly so it reads "established." No second accent, no gradients-as-decoration. Disciplined neutral ramp (borders #E2E8F0, secondary #334155, muted #64748B). Tight radii (4–8px, not 16px+ SaaS corners); prefer hairline borders + whitespace over heavy shadows.
- Motion: restrained reveal-on-scroll at most (short fade/translate, fast, once). No parallax, glassmorphism, blur, gradient-text, holographic effects.
- Mobile: mobile-first. The promise stays the dominant element at 380px (don't let it shrink below the subhead/CTA). Primary CTA reachable with one thumb in the hero, full-width tap target ~48px min, no horizontal scroll or zoom. Sticky bottom CTA bar one tap from the form. Capture form full-width single column, large inputs.

FINAL SELF-CHECK — NON-NEGOTIABLE. Do not return until every item passes; run a literal full-text scan across ALL output (page copy, headings, subheads, alt text, aria-labels, title/meta/OG tags, image filenames, CSS class/token names, code comments, data attributes, favicon/wordmark asset names), and report each surface as checked-clean:
- ZERO banned tokens anywhere: agentic, agent, canon, constellation, orbit, star, star systems. The AI layer is named only "AI intelligence layer" / "automated analysis" — never "agent"/"agentic" (the easiest accidental violation since the product IS the AI).
- Will is NEVER attorney/lawyer/counsel/"Esq."/"law license"/implied bar license — anywhere incl. meta/author tags. His title renders ONLY as "Chief IP Officer" / "fractional Chief IP Officer" / "Shadow CIPO." Confirm in hero, proof, footer, meta. The "strategy and judgment, not legal representation" line stays in BOTH the footer and the Honest Answers Q&A.
- ZERO pricing artifacts: no $, no "/mo", no "from", no struck prices, no "starting at," no currency anywhere incl. schema/meta. The three tiers render as named shapes only (Scout / Shield / Arsenal) with the intentional no-pricing line — never an empty/broken/TBD/bracketed slot.
- "Shadow CIPO" is glossed with "your behind-the-scenes Chief IP Officer" everywhere it appears prominently (minimum hero + footer); nav wordmark may be unglossed only if the hero gloss sits immediately below. The "shadow" framing reads capable/discreet/leverage in BOTH copy and visuals — never covert/secret/spy/noir/hooded/surveillance. Light/shadow as depth is allowed; sinister is a ship-blocker.
- NO astronomy motif anywhere — no starfields, orbits, galaxies, night-sky gradients, connected-dots-as-stars, twinkle, comet, nebula, or constellation-style line networks in backgrounds, dividers, icons, SVGs, hero, or loading states. Confirm the shadow/light hero is explicitly NOT a starfield.
- DRAFT banner present at the very top as a removable, unobtrusive top strip (not baked into the hero), exact intent: "DRAFT — staging preview. Copy pending Will sign-off; pricing and visual identity not yet approved."
- NO fabricated proof: no customer/partner logos, no logo wall, no invented metrics/stats beyond Will's three real anchors (15 years in IP monetization; led by Will Rosellini; raised a Series A himself), no fake testimonials, no stock "happy client" photography. Keep the deliberate logo-absence line, styled — not deleted as "empty."
- NO hard-coded domain (KonstellationAI.com or any other) in canonical/OG/meta/links — keep domain-agnostic. NO reference to or co-branding with "PatentVest" or any parent brand anywhere (copy, footer, meta, asset names). Shadow CIPO stands alone.
- No unsourced NIH/SBIR/STTR/TABA claims in page copy (audience context only).
- Colors resolve ONLY through --kit-* CSS variables; NO literal hex/rgb/hsl in any component markup or class. Flag any inline color as a constraint violation. All copy stays text/token-driven, never rasterized into images.

DELIVERABLE FORMAT
- A coherent design-token set as the single source of truth: CSS custom properties under :root named --kit-* (color, type scale, spacing, radius, one restrained elevation), each color with a stated value and one-word role, plus the Tailwind theme.extend.colors mapping to var(--kit-*) so utilities resolve to variables, not literals. State the one paper choice you committed to and why.
- Responsive, mobile-first components that drop into Next.js (App Router) + React + Tailwind, matching the §3–§4 section inventory: DRAFT banner, nav, shadow-hero, how-it-works, proof + pull-quote, tiers, Honest answers, capture form (empty + success states), footer. For each, state the mobile→desktop layout shift. Deliver mobile + desktop frames for the hero and the capture form.
- A short "what changed" note from pass 1: token set introduced, paper choice, brass contrast fix, type-scale + reading-measure additions, components made responsive/swappable, the hero rebuild, and any copy tightening — one line each so the operator can diff intent.

Tone: direct, specific, confident. Build it like a boutique advisory's design lead made it, not like a spec got satisfied.
