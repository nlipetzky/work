# KonstellationAI.com — Website Spec (Konstellation CIPO)

Output target of the `website-conversion-design` skill. Reads this venture's
context registry (`REGISTRY.md` and the spine artifacts under `canon/`, `revops/`,
`creative/`). **State: not started.** Held until the venture's open
questions resolve (domain ownership, offering definition). The skill provides the
conversion architecture; this file gets populated once there is a real offering to
sell and a confirmed domain.

Objectives when built: lead generation (qualified stranger → booked CIPO / IP
intake) and engagement (return reason for the audience). One brand truth, both jobs.

Do not write public copy before the offering is defined with Will (offer-extract)
and the brand store has positioning, voice, proof, and a conversion action.
