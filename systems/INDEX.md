# systems index

Each system is an indefinitely-operated agentic asset. Shared infra: canon-engine, revops-engine, projection-ui.

Generated by `scripts/generate-indexes.mjs`. Do not hand-edit.

### canon-crm-feed
Path: `systems/canon-crm-feed/CLAUDE.md`

The projection layer that pushes canon-engine state out to operator CRM surfaces (currently Airtable). Sits downstream of `revops-engine`'s staging → promotion pipeline: when a batch promotes into the canonical companies/contacts tables, t…

### canon-engine
Path: `systems/canon-engine/CLAUDE.md`

The studio's canonical knowledge graph. A Supabase Postgres project (`mzzjvoiwughcnmmqzbxv`, region us-east-2) that holds the system registry (`public.systems`, ~40 rows), asset inventory (`public.assets`, ~100+), the work-spine tables (`g…

### canon-ui
Path: `systems/canon-ui/CLAUDE.md`

Planned system. Provisional purpose: a dedicated knowledge-workbench UI for canon-engine that's heavier than projection-ui's "everything-cockpit" pattern can carry cleanly ... canon entity browse/edit, lineage views across `canon_artifacts…

### operating-sop
Path: `systems/operating-sop/CLAUDE.md`

This folder is a system repository for the Operating SOP system. It holds what the system is and how it is built. Launching Claude Code here loads this context and points the operator at THIS system.

### projection-ui
Path: `systems/projection-ui/CLAUDE.md`

The studio operator console. Next.js 15 app running on `localhost:4180` as a launchd service (`com.nick.projection-ui`, KeepAlive). The single human-facing surface for every other system: surfaces canon-engine state (work spine, registry, …

### revops-engine
Path: `systems/revops-engine/CLAUDE.md`

The RevOps execution engine: a Supabase Postgres (project `mrmnyscurmkfppicqqhk`, "revops-engine-dev") plus a set of Node runner scripts (`.mjs`) that move per-engagement data through a prep funnel and into a permanent, growing database. T…
