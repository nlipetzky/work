# Atlas kickoff: goal clarification + backlog reshape (2026-06-23)

Paste the block below as your first message to Atlas, or just tell Atlas "read this file and run it."

---

You're now my chief of staff ... your persona was updated in `practices/operator-os/CLAUDE.md` this
session, so read it first. I want to run a clarification-and-reshape pass on my goals and my work
backlog. Boris and I set this up; you take it from here.

Context to know (verify it against canon live ... don't trust this blind):

- My `/work` backlog is hollow and mis-prioritized. There are only ~6 open tasks in canon. The only
  one marked urgent is "Send Will's daily email," which is recurring and by our own methodology should
  be a system *activity*, not my #1 action. Most active projects have zero open tasks attached.
- The real work isn't captured. The operator-OS build (the system-building methodology, the
  `activities` run-layer schema, and the `/work` run layer ... all shipped this session) and the
  RevOps-engine build I want to focus on next do not exist as projects or tasks.
- I'm exiting lead generation as an offering. The Konstellation demand engine / Heyreach outreach work
  is not my edge ... treat it as a drop/demote candidate.
- canon now has a run layer: `public.activities` (each row a system's activity, with automation level
  / channel / architecture / ensured) and `systems.goal_id`. `canon-ingestion` is seeded as one
  worked example.

Run the pass the way your chief-of-staff persona defines it:

1. Lay out my 6 active goals and what's attached to each.
2. Run the hedgehog on each ... take a position on keep / rank up / demote / drop. Be opinionated.
3. Ask me the 2-3 things only I can answer (what I want to be best in the world at now, what's
   actually carrying revenue, and what "build the RevOps engine" concretely means).
4. Draft the reshaped goal set, plus the real projects and tasks for the goals that survive.
5. On my approval, write the changes to canon ... re-rank/drop goals, create the real projects/tasks,
   demote the lead-gen work, and move Will's daily email into an activity under a system.

Reference docs:
- `practices/operator-os/reference/methodology.md` ... the spine + leverage axis.
- `practices/operator-os/reference/system-building-methodology-draft.md` ... systems/activities + the
  run layer + architecture selection.
- `practices/operator-os/reference/spec-work-focus-surface.md` ... what the `/work` surface is for.

Start with step 1. Log the session at close so the next one can pick up.
