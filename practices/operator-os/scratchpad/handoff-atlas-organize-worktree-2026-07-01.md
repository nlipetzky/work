# Handoff → Atlas: organize the work-tree mess (2026-07-01)

From the Hermes / expert-liaison session. There are **64 uncommitted entries** on the shared `~/code/work` tree, spanning ~5 workstreams from multiple sessions. Your job: get this into clean, committed state without losing anything and without breaking anything.

## First: what is NOT at risk

- **DB is durable.** Migrations 024–034 are applied to live canon; the folder tables + the CIPO Will motion are live. Nothing in git can undo that.
- **Pushed baseline is safe:** `origin/ai-expert-folder @ bd428d9` holds Boris's folder + the expert-liaison engine + the operate cockpit.
- The risk is **only the uncommitted working-tree files** below.

## Hard rules (do not violate)

1. **NEVER `git clean -fdx`.** `.env` is gitignored and `-x` wipes it — project-breaking. (Known landmine.)
2. **NEVER `git add -A` blind.** Stage explicit paths per group.
3. **Do NOT commit** `accounts/ventures/konstellation-cipo/exports/` (PII CSVs) or `tsx-501/` (temp). Add both to `.gitignore` instead.
4. `systems/operating-sop/sops/launch-outbound-for-venture.ts` is **shared** (CIPO's 16-stage rewrite + the expert-signoff workflow) — commit it **once**.
5. **Deletions (`D`)** must be staged with `git rm` / `git add`.
6. **The tree is live** — another session is actively editing the ingestion files. Re-run `git status` immediately before staging, and do not commit a group that's mid-flight (see group 3).

## Branch decision — get from Nick before committing

Tree is checked out on **`konstellation-crm-ingest`** (local, HEAD `bd428d9`, unpushed). The pushed home is **`ai-expert-folder @ bd428d9`**. Ask Nick: land these commits on `ai-expert-folder`, or keep + push `konstellation-crm-ingest`?

## The groups (stage per group, explicit paths)

### 1 — Expert-liaison: SOP workflow + /operate embed + cold-outreach producer — READY
- `systems/canon-engine/supabase/migrations/034_register_cold_outreach.sql`
- `systems/canon-engine/scripts/request-copy-signoff.mjs`, `apply-copy-approvals.mjs`, and the edit to `produce-sequence.mjs`
- `systems/projection-ui/lib/operate/sop-types.ts`, `lib/queries/operatingSop.ts`, `components/operate/SystemViewEmbed.tsx`, `components/operate/ActivityDetail.tsx`, `components/operate/OperateCockpit.tsx`, `app/expert-liaison/ExpertLiaisonSurface.tsx`
- `systems/expert-liaison-engine/HANDOFF-build-2026-06-30.md`
- (the shared SOP file — commit with this group OR group 2, once)

### 2 — CIPO outbound pipeline — READY (exclude exports/)
- `systems/canon-engine/scripts/`: `find-contacts.mjs`, `verify-contacts.mjs`, `qualify-prospects.mjs`, `enrich-nih.mjs`, `gen-approval-surface.mjs`, `export-send-list.mjs` (new) + `enrich-prospects.mjs`, `watch-signals.mjs` (edited)
- `accounts/ventures/konstellation-cipo/artifacts/`: `copy-cipo-teardown-will-v1.md`, `hermes-brief-will-teardown-copy.md`, `email-draft-will-lead-magnet-decision-2026-06-30.md`
- `accounts/ventures/konstellation-cipo/CLAUDE.md`
- `systems/operating-sop/sops/launch-outbound-for-venture.ts` (the shared file, if not already committed in group 1)
- **EXCLUDE** `accounts/ventures/konstellation-cipo/exports/`

### 3 — canon ingestion / crm-ingest — CONFIRM FIRST (likely active WIP)
- `systems/canon-engine/packages/ingestion/src/google/auth.ts` (M)
- `systems/canon-engine/launchd/com.nick.canon-ingest.plist`, `systems/canon-engine/scripts/run-ingest.mjs` (new)
- `systems/projection-ui/app/api/staging/promote/route.ts`, `resync/route.ts`, `lib/queries/prospects.ts`, `package.json`, `.claude/launch.json` (M)
- `systems/projection-ui/lib/inngest/client.ts`, `functions.ts` (**D** — moved to `capabilities/inngest/`)
- This is the branch's namesake and appears to be a **live session**. Do NOT commit until that session/Nick confirms it's stable.

### 4 — OS structure: capabilities + INDEX regen + doctrine — READY (mostly generated)
- `capabilities/` (CLAUDE.md, INDEX.md, agents/, doctrine/, inngest/, schemas/, skills/, supabase-observability/)
- `practices/agentic-systems/reference/folder-architecture-decision.md`, `vertical-system-pattern.md`; `practices/agentic-systems/skills/ui-surface-discipline*`; the two `scratchpad/handoff-*.md`
- `accounts/INDEX.md`, `practices/INDEX.md`, `systems/INDEX.md` (auto-generated by `scripts/generate-indexes.mjs` — do not hand-edit; safe to commit as-is)
- `systems/canon-engine/CLAUDE.md`, `systems/canon-ui/`, `systems/revops-engine/workflows/`, `scripts/`, `.claude/hooks/`, `practices/revops/reference/source-mining-doctrine.md`, `systems/operating-sop/HANDOFF-wrapup-2026-06-29.md`
- `D practices/agentic-systems/ECOSYSTEM-MAP.md` (retired, replaced by the INDEX system — stage the deletion)

### 5 — Root/system CLAUDE.md context edits — READY
- `CLAUDE.md`, `systems/canon-crm-feed/CLAUDE.md`, `systems/projection-ui/CLAUDE.md`

## Recommended sequence

1. `git status` (re-check — tree is live).
2. Confirm the branch with Nick.
3. Add `exports/` + `tsx-501/` (and any cache/build dir) to `.gitignore`; commit `.gitignore`.
4. Commit groups **1, 2, 4, 5** as scoped commits (explicit paths). The shared SOP file lands once.
5. **Hold group 3** until the ingestion session says it's stable.
6. Push the branch Nick chose.

## Related

- Closing the Boris + CIPO sessions: `practices/agentic-systems/scratchpad/handoff-close-boris-cipo-sessions-2026-07-01.md`.
- Nothing here requires touching the DB. If in doubt on a file's owner, route it to Nick rather than guessing — better a slower commit than a lost or wrongly-attributed one.
