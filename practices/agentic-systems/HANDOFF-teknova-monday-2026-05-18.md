# HANDOFF: Teknova AAV — Monday 2026-05-18 (fresh session)

**For:** a fresh agentic-systems (Boris) session. Self-contained. Do not rely on prior chat. Read the canonical docs below; they are source of truth.

## Role (do not drift)

agentic-systems operator. You own: artifacts, client-facing framing, the criteria/vocabulary, SPEC/design review, and the domain-expert review loop. You review and decide; you do not improvise client messaging or fire spend. Verify against the surface, never infer ("workflow completed" ≠ done; "looks fine" ≠ verified).

## Where this stands (verified 2026-05-15, surface-confirmed)

**The complete classified company set is DONE and v3-validated.** L2 workflow `rXKuqfDwqX7TYzxK` v`bb1a0114` ran the full 631: **0 needs_verification; 35 surfaced (AAV confirmed), 11 rejected, 585 borderline (held, conservative by design).** Nkarta keystone holds (rejected, vasculitis reason). Numeric-injection closed empirically. Throttle held (zero 429). This is stage 1 of the locked vocabulary — companies only, no enrichment, no contacts.

## Read first, in order

1. `~/.claude/plans/we-are-aligned-draft-zany-hamming.md` — canonical plan + **LOCKED Vocabulary block** (classified company set / enriched company set / outreach-ready cohort; never say "complete cohort" without specifying which).
2. Memory: `project_teknova_l2_state_and_deferred.md`, `feedback_teknova_icp_fixation_pattern.md`, `protocol_domain_expert_review_loop.md`, `project_teknova_decision_authority.md`, `feedback_persist_deferred_work.md`, `feedback_client_optimizer_not_gate.md`.
3. `/Users/nplmini/code/work/accounts/clients/teknova/MEETING-protocol-card-2026-05-15.md` — run today's meeting from this (file dated 2026-05-15; content current, the meeting is today).

## Resume point — Monday, in order

1. **Pre-read is ready, not yet sent.** Artifact = `/Users/nplmini/code/work/accounts/clients/teknova/ellie-prereview-pack-2026-05-15.pdf` (HTML + markdown sources alongside). Email draft = `/Users/nplmini/code/work/accounts/clients/teknova/ellie-email-2026-05-15-prereview.md`. **Nick action before send:** spot-check one PDF link opens its clinicaltrials.gov record; attach/host the PDF; replace `[LINK]` (or set "attached"); send reply-all on the "New AAV discovery approach" thread.
2. **30-min Teknova meeting today (Ellie/Christa/Jenn).** Run from the protocol card: pre-frame the error budget first; route every "this is wrong" through the 4 lanes; never debug live; recognition-not-permission staged framing; Ellie=output, Jenn=direction.
3. **After the meeting:** capture Ellie's calls (esp. "outside our profile" → feeds the ICP/fit layer; segment calls; eliminations). Inbound synthesis to next artifact version.
4. **Step 5 spend gate** (enrichment of the ~35 surfaced) — Nick's explicit same-session call, small now. Nothing spends before it.

## Deferred open items (do NOT block Monday — tracked in project_teknova_l2_state_and_deferred.md with triggers)

- Duplicate run-log rows in L2 → fix before runs become routine (ships through the dangerous update_workflow path).
- PTC duplicate company rows → resolve before/within the Ellie-view step.
- No ICP/fit gate after the AAV gate → the explicit next layer, built from Ellie's "outside our profile" input.

Surface these proactively when a session touches the triggering area. Do not rely on the suggested-task chip.

## Hard rules

- No spend without Nick's explicit same-session approval. Handoff/prior context never authorizes it.
- Any L2 workflow change ships through the n8n-safe-update protocol + gate #1 (PATCH-body read-back; builder corrupts on update_workflow; HTTP-PATCH + verify field-by-field).
- Client artifacts: no internal jargon/IDs; lead with verifiable proof; never anchor on mega-pharma; AAV-confirmed ≠ ICP-vetted; no date promises ever; recognition-not-permission.
- Verify on the surface, never assert from inference. Proof beats narrative.

## First action

Read the canonical docs + memory. Confirm the resume point. Then either help Nick get the pre-read sent and prep the meeting from the protocol card, or, if the meeting has happened, run inbound synthesis of Ellie's calls. Do not build, run, or spend without confirming with Nick.
