# Session handoff ... 2026-06-29 → next session

## Session arc

Started with a folder-architecture question (cross-folder discoverability), shipped the static-index fix (slice 1 of the verdict), then re-architected `/operate` into the universal three-mode cockpit (RUN / ITERATE / BUILD). Plan written, approved, and three slices executed end-to-end.

## What's locked in

### Architecture decisions (in memory + reference docs)

- **Vertical-by-system**: systems are the unit of ownership; capabilities are the shared library. Reference: [folder-architecture-decision.md](../reference/folder-architecture-decision.md). Memory: `project_folder_architecture_decision`.
- **Inngest mount pattern**: client in `capabilities/inngest/`, functions per-owning-system in `workflows/`, projection-ui unions and serves at `/api/inngest`. Memory: `project_inngest_mount_pattern`. Pattern doc: [vertical-system-pattern.md](../reference/vertical-system-pattern.md).
- **Three modes for /operate**: RUN / ITERATE / BUILD, each with its own folder-persona, skill bundle, and AI context. Maps to `feedback_three_modes_run_iterate_build` organizing model. Approved plan: `/Users/nplmini/.claude/plans/yes-lock-in-the-iridescent-book.md`.

### UI design (wireframes + spec)

Persisted at [systems/operating-sop/reference/wireframes/](../../../systems/operating-sop/reference/wireframes/):
- `README.md` ... canonical design intent for all three modes
- `operate-run-v1.html` ... the first ship-quality RUN render
- `operate-run-variant-a.html` + `operate-run-variant-b.html` ... density variants for reference

Not persisted as HTML (rendered inline in chat, captured in the README spec): RUN v2, RUN v3 (current canonical), ITERATE, BUILD. Re-render from the README spec when needed.

### Code shipped

**Slice 2A (foundation + RUN refactor) — DONE, verified live with real spawn + persona output**

- `systems/operating-sop/personas/{run,iterate,build}/` ... three folder-personas with CLAUDE.md + manifest.json
- `systems/operating-sop/lib/spawn-claude.ts` ... mode-aware dispatcher (ENV composition, symlink-farm skill bundle, osascript spawn)
- `systems/operating-sop/.claude/{hooks/persona-loader.mjs, settings.json}` ... SessionStart hook auto-loads persona into spawned Claude sessions
- `systems/projection-ui/app/api/operate/open-claude/route.ts` ... loopback-only mode-aware spawn endpoint
- `systems/projection-ui/components/operate/ModeToggle.tsx` ... three-segment toggle, URL ?mode=
- `systems/projection-ui/lib/operate/mode-features.ts` ... single source of truth for per-mode flags (spine reorder, composition editable, evals authoring, action buttons)
- `systems/projection-ui/components/operate-inspector.tsx` ... mode-aware "Open in Claude Code (RUN|ITERATE|BUILD)" button, CompositionPanel mounted below

**Slice 2B data + persona + read-path UI — DONE**

- Migrations applied in canon (`mzzjvoiwughcnmmqzbxv`): 017_sop_skills_registry, 018_sop_spine_tables, 019_sop_activities ... 8 tables live
- `systems/canon-engine/scripts/sync-skills.mjs` ... walks SKILL.md files into canon.skills (16 skills synced, including 5 new ITERATE skills)
- 5 new ITERATE SKILL.md files under `systems/operating-sop/skills/{historical-run-reader, eval-reviewer, skill-quality-critic, prompt-tweak-proposer, regression-fixture-author}/`
- `systems/operating-sop/lib/historical-runs.ts` ... cross-engagement run-digest loader (graceful on missing tables)
- `systems/projection-ui/lib/queries/sopComposition.ts` ... `getActivityComposition`, `getActivityRuns`, `getActivityEvals` (graceful on missing tables; PostgREST PGRST205 + Postgres 42P01 both caught)
- `systems/projection-ui/app/api/operate/sync-canon/route.ts` ... one-shot bootstrap from TS bundle (1 SOP, 15 stages, 1 workflow, 8 nodes, 8 sop_activities live in canon)
- `systems/projection-ui/app/api/operate/composition/[activityId]/route.ts` ... composition GET endpoint
- `systems/projection-ui/components/operate/CompositionPanel.tsx` ... renders Composition / Provenance / Runs / Evals under Inspector

**Migrations + capture items + small data fixes**

- canon.systems gained a row for `canon-engine` (was missing despite being a real system)
- root `~/code/work/CLAUDE.md` slogan + capabilities rubric aligned across all docs
- 4 capture items dropped to Atlas's inbox earlier in the session for systems/capabilities/engagement/studio-thesis gaps

## What to pick up next session

The editable ITERATE path (the remaining 2B UI work):

1. **CompositionPanel becomes editable when `mode === 'iterate'`** ... swap `<span>` for `<input>` / `<select>` on function path, trigger, schemas in/out, adapters chips, skills.
2. **Skill swap panel** ... when an operator clicks a skill chip in ITERATE mode, expand an inline panel listing candidate skills from `canon.public.skills` (filter by status='active' or 'draft', sortable by owner_system_slug). Selection swaps the chip; queues an unsaved iteration draft.
3. **/api/operate/iterate save route** ... POST writes the draft to `canon.sop_activities` with `version = current + 1, is_current = false`. Stays draft until BUILD-mode publish flips it.
4. **"Save iteration" / "Run after save" / "Discard" action buttons** in the action row (gated by `FEATURES.iterate.actions.save_iteration` already in the table).

After that, slice 2C (BUILD mode + sop-writer + migrations 020-023 + Publish flow).

## Verification quick reference

- `/operate/launch-outbound-for-venture` ... full cockpit page; click the mode toggle to see accent + button label rotate
- `/operate/launch-outbound-for-venture?stage=s9-build-the-list&node=n5` ... scroll Inspector to see the new Composition panel
- `curl -X POST http://localhost:4180/api/operate/open-claude -d '{"mode":"run","sopId":"...","engagementId":"konstellation-cipo","runId":"..."}'` ... spawns Terminal with auto-loaded persona context
- `curl -X POST http://localhost:4180/api/operate/sync-canon` ... re-bootstrap canon from TS bundle (idempotent)
- `node /Users/nplmini/code/work/systems/canon-engine/scripts/sync-skills.mjs` ... re-sync skill registry

## Known gaps + gotchas

- **Activity column `skills`/`schemas`/`adapters`/etc. are all NULL** in canon.sop_activities today. The TS bundle doesn't populate them yet. They'll fill in as activities get iterated/built through the editable affordances (slice 2C+) or via a one-shot enrichment pass.
- **Migrations 020-023 not yet applied** ... `activity_runs`, `activity_evals`, `activity_eval_runs`, new artifact types, publish RPC. CompositionPanel renders graceful "lands in slice 2C" messages where these would surface data.
- **Two scratchpad locations exist for this session** ... `/private/tmp/claude-501/.../.../scratchpad/` (ephemeral) and `practices/agentic-systems/scratchpad/` (this file, persistent). The wireframe HTMLs we kept got copied to `systems/operating-sop/reference/wireframes/` permanently.
- **`canon-engine` was missing from `canon.public.systems`** ... added this session. Worth checking if other system slugs referenced by future bundles are also missing.
- **`projection-ui` is also not in canon.systems** ... not blocking today but worth registering eventually.

## Plan reference

Full approved implementation roadmap: `/Users/nplmini/.claude/plans/yes-lock-in-the-iridescent-book.md`

The plan is segmented into:
- Slice 2A: RUN refactor + foundation ✅
- Slice 2B: data + ITERATE persona/skills + ITERATE UI (read path ✅ / editable path TODO)
- Slice 2C: BUILD mode + sop-writer + Publish flow + migrations 020-023
- Slice 2D: tab absorption (per-tab port to embedded SystemView) + nav swap

## Memory entries to read first next session

- `project_folder_architecture_decision` ... why we chose vertical-by-system
- `project_inngest_mount_pattern` ... how systems' workflows compose
- `feedback_three_modes_run_iterate_build` ... the organizing model
- `project_operating_sop` ... operating-sop's purpose + status
- `project_cipo_pipeline_state` ... the real-world test case (konstellation-cipo SopRun)
