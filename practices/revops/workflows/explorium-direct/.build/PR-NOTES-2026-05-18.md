# PR notes — L2 R5 fix + Step 9 Verify (2026-05-18)

Status: built, brain-validated offline, **NOT deployed, NOT run**. Returned for agentic-systems review before any n8n push or run. CT.gov reads only — zero spend so far.

## Artifacts
- `.build/r5-trial-test-2026-05-18.ts` — shared R5 module (single source of truth; embedded verbatim in both workflows).
- `.build/L2-applyRules-R5-2026-05-18.js` — FULL replacement block for the L2 "Apply Rules" node (select-all-paste).
- `.build/L2-rebuild-2026-05-18.ts` — L2 SDK build (workflow `rXKuqfDwqX7TYzxK`); only Apply Rules changed; 26-node topology identical to 05-15.
- `.build/Verify-build-2026-05-18.ts` — NEW Step 9 Verify workflow SDK (single linear path).
- `.build/verify-diff-2026-05-18.mjs` — offline regression harness (CT.gov only, no Airtable/n8n/spend).

## Decisions taken (within stated latitude)
1. **Approach (b):** L2 re-fetches cited NCTs from CT.gov v2 at classify time, in-node via `this.helpers.httpRequest` (no new nodes, topology intact, lower n8n-safe-update risk). Shared fetch+test module reused verbatim by Verify.
2. **Build-1 fetch scoped to the would-surface path only** (canonical/clause-A). Non-surface routes unaffected. Fail-closed fetch budget `MAX_FETCH=800` (would-surface set is ~35–60; overflow = data anomaly → hard stop before any PATCH).
3. **NCT delimiter:** comma-space in field `CT.gov NCT IDs` (`fldD2kbSLk3h0YlSp`); parsed robustly with `/NCT\d{8}/gi`.
4. **`Run Type=verify`:** rides `options.typecast:true` on the receipt write (existing Write Run Log pattern); no manual schema change needed. `Verification Verdict` options (Confirmed / Not confirmed / Needs review) already exist — no typecast reliance there.
5. **Clause-3 token scope = trial title OR intervention name** (per ORACLE-CORRECTION point 2). Required: Bayer NCT03588299's AAV proof is in the title, not the intervention (`DRUG: BAY2599023 (DTX201)`); intervention-name-only would regress a MUST-Confirm row. Negative-intervention filter (SoC/replacement/placebo/sham/diagnostic/device/OTHER-only) is what kills NCT03587116 and NCT05568719 while letting Pfizer's real AAV trials pass.

## Deviation flagged for review
**L2 `Update Needs Review` PATCH body changed.** v3 set only `Enrichment Status=needs_aav_review`. Under R5, route 4 is now heavily populated (every would-surface company with no passing trial). I added `Verification Status="borderline"` + `Rejection Reason` so these rows reach a terminal state — otherwise they stay `needs_verification` and (a) break the "needs_verification=0" completeness invariant and (b) get re-processed every L2 run. This is the one PATCH-body change vs the accepted v3; all other 6 PATCH bodies are byte-identical. Please confirm `borderline` is the intended terminal state for R5 needs-review (matches the evaluator's `verification='borderline'`).

## Harness result (live CT.gov, 2026-05-18)
All hard criteria PASS:
- Pfizer → Confirmed **via NCT03362502** (not the weak NCT03587116) — evidence integrity holds. Ultragenyx → Confirmed via NCT04884815.
- 22/22 in_record_machine → Confirmed (Bayer reproduces on title).
- 3/3 no_nct → Needs review.
- Per-trial guard: NCT03587116 fails c3 (SoC-only), NCT04909346 fails c1 (OBSERVATIONAL).

Expected divergences only (non-blocking, all predicted by the correction doc): 4 domain_knowledge rows (MavriX, Tenaya, Beacon, 4DMT) legitimately flip to Confirmed on a non-first cited NCT — the "test all NCTs" behavior working as intended. Vitalgen/Prevail → Not confirmed (AAV proof only in summary, out of R5 scope). Baxalta → Confirmed (ICP gate's job, not Verify's). Precision BioSciences → Not confirmed (gene editing, conservative).

## Carried-forward constraints (unchanged)
- No deploy/run/spend without explicit go. Deploy = n8n-safe-update: `update_workflow` wipes creds + false-500; reattach `may 26 all bases` to all credentialed nodes, republish draft, verify by read-back.
- Gate #1 before any full run: deployed-config PATCH-body read-back + small real smoke (CSL Behring NCT03569891 → Confirmed; Pfizer NCT03587116 → c3 fail) before the full surfaced set.
- Build 1 re-run from trigger re-blanks + reclassifies all 631 (idempotent for correct rows).
- Knock-on (out of this build's scope, flagged): `accounts/clients/teknova/methodology-outline-2026-05-18.md` needs correction — Pfizer/Ultragenyx are AAV, excluded on size, not modality.
