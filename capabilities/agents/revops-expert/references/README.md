# revops-expert references

The RevOps AI-expert-folder is the first tenant of the AI-expert-folder capability (design item #1 of the operating-sop agenda).

## Design + build plan
`~/.claude/plans/this-is-a-good-ticklish-bunny.md` ... the full spec: the three unit kinds (recipe/option/ruling), the judgment-unit ledger, the standing gate, the Hermes seam, and the phased build.

## Canon schema
Migrations `028_expert_folders` through `032_folder_seam_rpcs` in `systems/canon-engine/supabase/migrations/`.

## The stewardship frame
SOP = a function; expertise = the libraries; account = the call-time args. This folder IS the RevOps libraries. The SME is a bootstrap, not a battery; the human migrates source → frontier-pusher → optimizer and contributes less on purpose. Related doctrine: the live-bound iteration loop, the three-modes model, and the expert-liaison membrane (Hermes).

## Composition
The one domain-agnostic SOP steward (`systems/operating-sop`, BUILD mode) composes a RevOps SOP by referencing this folder's active recipes into stages and binding its active options/rulings onto the composed activities. Conflicts in domain truth route to Hermes; conflicts in form the steward integrates (overlay beats backbone).
