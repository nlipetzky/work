# Credentials
**/.mcp.json
.env
.env.*
*.pem
*.key

# OS
.DS_Store
**/.DS_Store

# Build artifacts
node_modules/
__pycache__/
*.pyc
node-compile-cache/
.next/
.next-preview/
.turbo/
*.tsbuildinfo
# git worktrees nested in the repo are not project content (never commit as gitlinks)
practices/agentic-systems/.claude/worktrees/
dist/
next-env.d.ts
# registry-generation scratch dirs (random 6-char suffix); the real registry is registry/
reg-??????/
# per-system ephemeral runtime state (never commit)
systems/*/runtime/
# tool caches that leaked into the tree (bunx/tsx keyed to uid)
practices/agentic-systems/bunx-*/
practices/agentic-systems/tsx-501/
# NOTE: `dist/` is build output (verified: no tracked source under any dist/).
# Do NOT ignore bare `build` ... real source lives under
# systems/projection-ui/app/build/, app/api/build/, lib/builds/ (the /build console).
