# HANDOFF: Teknova / RevOps — fresh session (2026-05-19)

**For:** a fresh agentic-systems (Boris) session. Self-contained. Verify against the observable surface; agent narration (including this doc) is hypothesis until surface-checked. **SUPERSEDES** `HANDOFF-teknova-revops-currency-gate-2026-05-18.md`.

## Role / posture (do not drift)
You are Boris, orchestrator. Builders (Explorium-Direct, Workflows) execute via plans you write; Nick relays to/from them for now and is the authorization gate for spend and irreversible actions. You review and independently surface-verify every builder report — "success" ≠ delivered. **Talk to Nick in plain English. No jargon, no acronym-puzzles — he flagged this many times.**

## BIGGEST STRUCTURAL CHANGE THIS SESSION — read first
The **System Registry** Airtable base `apppQjlZiktpbO4aX` now exists and is the system-of-record. `practices/revops/workflows/REGISTRY.md` is a tombstone pointing at it. Tables: **Operating Model** (read-first, tells a cold agent how to use the base), **Systems**, **Assets**, **Roadmap**. Bound operating manual: `practices/agentic-systems/system-registry-operating-manual.md` (keep base ↔ manual in sync same-turn). Registered systems: `teknova-enrichment` (client, forming), `revops-engine` (platform, emerging), `expert-liaison` (platform, emerging). Read the base's Operating Model table before acting on it. Many new canon entries this session in `practices/agentic-systems/canon/canon-log.md` (+ sources/) — read it; it carries the hard-won primitives below.

## Workstream state

1. **L1 → Company Events signals — DONE, verified.** L1 `9gcmEjq1lvOY2jZS` (11 nodes) writes 186 clinicaltrials.gov trial signal rows to Company Events, idempotent on NCT, Company link set. Registry L1 asset reconciled=true. `active=false`.
2. **L2 currency Phase 1 — DEPLOYED, proven INSUFFICIENT.** L2 `rXKuqfDwqX7TYzxK` v4 R5+currency. Selective run confirmed: eyeDNA (CT.gov-terminated) → discontinued → needs-review (correct); **Pfizer & Adrenas still surfaced as `current`** because currency was single-source (CT.gov lags press). Documented limit. Superseded by workstream 3.
3. **Currency completion (A+B+C) — CLOSED + VERIFIED 2026-05-19 (exec 80822).** Pfizer `rec83lbbxLTPi84zv`: Verification `borderline` (off surfaced), Currency `discontinued` from Perplexity trade-press, `needs_aav_review` — false-positive closed. Adrenas `recA4rY40iqwtNVJP`: `surfaced` / Currency `current` (live NCT04783181); `unknown` signal correctly did NOT override CT.gov — precedence proven precise. Bounded path executed clean: L2 filter narrowed→restored exactly, 13/13 creds intact, no node change, flags cleared, inactive. agentic-systems independently re-pulled both records + workflow — E-D report matched surface. **Explorium-Direct session closes.** Open nuance (rules-tuning, NOT a reopen): Pfizer routed to `borderline` vs literal `needs_review` — functionally equivalent (off-surfaced + human-gated). Plan: `practices/revops/workflows/explorium-direct/PLAN-currency-completion-2026-05-19.md`. Detail below retained for history.
   - **A (L2 trade-press precedence) — verified.** L2 `rXKuqfDwqX7TYzxK` 27 nodes, `Apply Rules` v5 (R5+currency+trade-press, 230 lines), `Read Trade Press Signals` node inserted with filter `AND({Is Latest}=1,{Event Type}='program_status')`, conn `Read Candidates → Read Trade Press Signals → Apply Rules`, Read Candidates static selective-run formula intact. `active=false`. Surface-verified 2026-05-19.
   - **C (L1 normKey dedup) — verified.** L1 `9gcmEjq1lvOY2jZS` 11 nodes unchanged, `Merge Discovery Sources` + `Resolve & Explode Trials` carry v2 markers. `active=false`.
   - **B (Perplexity trade-press source `wIyuFELxzXMgHCDV`) — STRUCTURE FIXED+VERIFIED; ALL CREDENTIALS WIPED. DO NOT RUN until creds re-attached.** E-D corrective PUT (2026-05-19) fixed the structural defects, surface-verified: "Write Company Events" → Company Events `tblnzX2b2kqNGzW6r`, upsert on `External ID`, all 11 plan Step 3 fields, `Is Latest=true`+`Event Type='program_status'` (satisfies L2 filter); "Write Run Log" → `tblEVSEqetmu4ScHe` (= L1's run-log table, cross-checked); test filter `OR({Company Name}='Pfizer',{Company Name}='Adrenas Therapeutics Inc')`. **NEW BLOCKER: E-D's "credential-preserving" PUT wiped ALL 4 node credentials** (Read Surfaced Companies, Write Company Events, Write Run Log, Call Perplexity all `credentials:{}`). E-D misread `{}` as "REST API hides creds" — false; the PUT cleared them. **Nick must re-attach in n8n UI before any run:** 3 Airtable nodes → "may 26 all bases"; Call Perplexity → Perplexity API cred. (Stray USPTO-cred defect now moot — all removed.) **Canon to capture at close-out:** even a REST PUT wipes creds unless credential objects are carried in the payload; an empty `credentials:{}` in a GET is the wipe, not API opacity. Owner: Explorium-Direct / Nick (UI). Ticket: `practices/revops/workflows/explorium-direct/PROMPT-taskb-corruption-fix-2026-05-19.md`.
   - Pfizer/Adrenas confirmed `needs_verification` (seen in exec 80796 runData, not surfaced).
   - **TEST EXEC 80796 (2026-05-19 17:41) RAN GREEN, DATA FAILED. Identity lost across HTTP node.** Perplexity answers correct (Pfizer=DISCONTINUED w/ source; Adrenas=UNCLEAR). But `Call Perplexity` (httpRequest) replaces the item, so `_companyRecordId`/`_companyName` from `Attach Record ID` are gone by `Parse & Prepare Signal` → `externalId="undefined:program-status"` for BOTH, `companyRecordId=None`. Result: both upserts hit the SAME row `reci8iIb1WqKwz39V` (collision; Adrenas/unknown overwrote Pfizer/ended), no `Company` link → L2 can't join it. Currency does NOT work end-to-end. Corrective ticket OUT: `practices/revops/workflows/explorium-direct/PROMPT-taskb-identity-loss-fix-2026-05-19.md` (fix: read id from paired `$('Attach Record ID')` item, not HTTP response). Owner: Explorium-Direct. **DO NOT run L2. Pfizer/Adrenas NOT closed.**
   - **Debris to clear (pending Nick OK — destructive):** orphan Company Events row `reci8iIb1WqKwz39V` (External ID `undefined:program-status`, no Company link, `Event Type=program_status`,`Is Latest=true`) WILL match L2's `Read Trade Press Signals` filter as noise. Delete before any L2 run.
   - **Task B identity fix VERIFIED (exec 80797, agentic-systems re-pulled).** Two distinct Company Events rows: Pfizer `recDUb4YEM3xneRLC` (`Vitality=ended`, External ID `rec83lbbxLTPi84zv:program-status`, Company linked), Adrenas `recF7fK0yQqelOsIO` (`Vitality=unknown`). Filter restored to `{Verification Status}='surfaced'`, all creds intact, inactive. Task B is done.
   - **FINAL currency step = L2 selective re-run.** Ticket: `practices/revops/workflows/explorium-direct/PROMPT-l2-rerun-close-currency-2026-05-19.md`. Owner: Explorium-Direct. Gate: E-D reports the `needs_verification` count BEFORE running (decides bounded vs as-is); no L2 node edits unless that count forces it (cred-wipe risk; static filter only). DoD: Pfizer `rec83lbbxLTPi84zv` drops off `surfaced`/`current` (→ needs-review/discontinued via the `ended` signal); Adrenas `recA4rY40iqwtNVJP` NOT demoted by its `unknown`. agentic-systems re-pulls Companies to verify. **On clean verify, currency is finished and the E-D session closes.** L2 is not paid — no spend gate.
4. **Contact-sourcing — 3 fixes DEPLOYED+VERIFIED; blocked on Anthropic BILLING (not credential) + Nick auth.** `bYZ0sAzyUvU60wMZ`, versionId `d15379e9…`, 23 nodes, inactive. Surface-verified 2026-05-19:
   - Fix 1 (recursive employer norm, holdings|holding|plc|group|limited|international do/while) — on both `Email + Employer Verify` and `Apply LinkedIn Result`. ✓
   - Fix 3 (Apify 25-concurrent overflow silently dropped 10/50) — `Apify LinkedIn Verify` native batching `batchSize:1, batchInterval:200`. ✓
   - Fix 4 (blank-email upsert collision) — `Prepare Contacts Upsert` skip is `if (!d.email) continue;`. ✓
   - **Fix 2 RECLASSIFIED — NOT an unattached credential.** `Residual ICP Score` already has `anthropicApi` bound (id `k6pMUap0…`, "Teknova. Konstellation"); surface-confirmed. Root cause is account-level **"credit balance too low"** (exec 80786 contextData). **ACTION: Nick adds Anthropic billing credits at console.anthropic.com → Plans & Billing. NO n8n UI action. The old "attach the Anthropic credential in n8n" item is WRONG — do not chase it.**
   - Anthropic billing fixed (Nick added credits); model `claude-sonnet-4-5` — bump only if scores stay 0.
   - **Pipeline works end-to-end. No silent drop** (the "5→2" / "low email coverage" reads were agentic-systems misreads of IF-branch runs + batch-of-10 output — corrected; exec 80794 wrote all 5). **Apollo is NOT exhausted** (Nick confirmed credits) — `Apollo People Match` returns empty due to a query/params bug.
   - **STATUS 2026-05-19 (versionId `b30089a2`, exec 80824, agentic-systems re-pulled):** DoD2 (Apollo returns matches) **MET** — 5/5 real matches via linkedin_url fix. DoD3 (four honest states) **logic working** — all 5 correctly `Unverifiable`/`explorium`, not laundered. DoD1 (Hunter-first) **BLOCKED ON DATA, not code:** Target Companies MeiraGTx row has `Domain=None`/`Website=None` → `targetDomain` null → Hunter emailFinder bad-requests all 5 → fallback to explorium. Hunter code correct, starved. **Unblock = set MeiraGTx `Domain=meiragtx.com` (DATA fix, reversible), re-run bounded size:5, agentic-systems verifies DoD1.** Decisions: (Q1) do NOT add email-pattern domain derivation — circular, reintroduces guessed-email trust problem, scope creep. (Q2) merge-topology cleanup OUT OF SCOPE — two-chain run is functional, no drop. **Systemic precondition (separate from closing this WF): Hunter-first makes `Domain` a hard dep; Target Companies rows must have domains before any non-test run — data hygiene task.**
   - **CLOSED 2026-05-19 — all 3 DoD verified on exec 80827 (agentic-systems re-pulled).** MeiraGTx Domain data fix applied (`rec7qaGedk0dmOON3` → meiragtx.com, no workflow edit, versionId still `b30089a2`). targetDomain=meiragtx.com, Apollo 5/5, Hunter returned all 5 (no bad-request), all `emailSource=hunter`, status `Catch-all (unconfirmed)`, idConf=False. Honest limitation (NOT a defect, flag to Ellie): meiragtx.com is a catch-all domain so Hunter cannot verify any address there → catch-all companies will only ever yield `Catch-all (unconfirmed)` contacts. Workflows session closed. Prior deploy ref (superseded): versionId `2ed28981`.
   - **REOPENED — employment-currency defect (human spot-check caught it).** Email-trust scope closed but a NEW must-fix surfaced: `Employer Match Confirmed=true` for Girish Chitnis who left MeiraGTx ~April 2026. Root cause: confirmation = Explorium/Apollo last-known-employer agreement; LinkedIn freshness check SKIPPED when both stale sources agree. Same disease as catch-all/currency. Also: email-keyed upsert spawns duplicate rows per person (8 rows = 5 people); workflow writes no opt-out field (Teknova compliance req). Bounded ticket: `practices/revops/workflows/PROMPT-contact-sourcing-employment-trust-2026-05-19.md` — 5 items: (1) capture-wide ALL third-party payloads [canon capture-wide-derive-narrow], (2) employer-confirmed must mean *currently* employed via LinkedIn signal, LI check runs ESPECIALLY when sources agree, (3) honest employment status field for Ellie, (4) re-key upsert on stable per-person id (LinkedIn URL) + reconcile existing dupes, (5) preserve+populate opt-out. **Regression oracle: Girish Chitnis must read NOT-currently-employed.** Owner: Workflows. Verification = bounded MeiraGTx size:5, Nick go, agentic-systems surface-verifies. **Canon to capture at close-out: a confirmation/verification status must be backed by a currency signal, not source agreement — stale-source agreement is the highest-risk case, not the safest (people-employment instance of observable-surface/currency family).**
   - **Contacts table cleanup — Nick decided OK to delete (Supabase-imported, also in Salesforce); agentic-systems recommended snapshot + verify-sync-sticks first but deferred to Nick. NOT agentic-systems-executed.** Original pushback findings retained below.
   - **Contacts table cleanup — ON HOLD, do NOT bulk-delete.** Nick proposed deleting all legacy contacts (suspected Supabase hiccup imports); agentic-systems pushed back. Surface findings (tblWJksRL1yKSUgrm, 385 total): **61 `engaged`, 17 `cadence_ready`** — meaningful real outreach state, provenance unconfirmed (act-on-hypothesis risk). Sync topology (Supabase ↔ this table ↔ Ellie's Outreach base) NOT confirmed — delete may cascade/boomerang. Recommended path: provenance-flag + filtered view for Ellie now (non-destructive); confirm sync wiring; only then scoped delete of confirmed-legacy AND non-engaged rows with a snapshot first. Open: sync-topology verification. (1) Apollo body adds `linkedin_url` conditionally; (2) `Email + Employer Verify` no longer selects email (passes `apolloEmail` raw); (3) `Hunter` op=`emailFinder`; `Apply Email Verify` Hunter-first + exactly the four states `Verified`/`Catch-all (unconfirmed)`/`Unverifiable`/`Not found`. 23 nodes, inactive, size:5, all 10 creds intact. Ticket: `practices/revops/workflows/PROMPT-contact-sourcing-email-trust-2026-05-19.md`. **Lane note: Workflows session did the Workflows ticket — NO lane crossing (an earlier agentic-systems "E-D did wrong ticket" alarm was a false read off a label; retracted).** **REMAINING: bounded MeiraGTx size:5 real run (Nick same-session go) → agentic-systems verifies on surface: Hunter-sourced emails w/ truthful `Email Provider Source`, Apollo non-empty for matchable, four-state status, no catch-all/unverifiable marked `Email Identity Confirmed`. That run closes this workflow. No 50-run, no new scope.** Owner: Workflows.
5. **Ellie-facing trial narrative — deferred.** Logged on expert-liaison roadmap. NOT in any current build. Deterministic L1/L2 code nodes stay code; narrative is a downstream derived projection on surfaced companies only.
6. **Selective-run protocol — established + implemented on L2.** `practices/agentic-systems/reference/revops-engine-selective-run-protocol.md`. `Run Selected` checkbox on Companies. Filter MUST be a STATIC Airtable formula (`OR({Run Selected}=1,{Verification Status}='needs_verification')`), NEVER a dynamic n8n expression (that was a live failure).

## Hard rules a fresh session must carry
- **Never** n8n-mcp `update_workflow` on L1/L2/contact-sourcing — it wipes credentials. Use the credential-preserving REST API PUT, and capture full workflow JSON first + verify node count/connections immediately after (a malformed PUT nearly destroyed L1).
- Raw read-back after any workflow change; `validate_workflow` misses Airtable-node corruption.
- Verify on the surface. A "success" execution with an empty output table is a failure (this happened with contact-sourcing).
- No autonomous spend. Paid re-runs (Explorium, Perplexity) are STOP gates → Nick, same session.
- Never re-author the segment artifact or persona rows — the persona-projection session owns them. Patient-facing disqualifier: accept-the-gap until that session adds it.
- Registry is system-of-record; keep base ↔ manual in sync; tombstone, never silently delete.

7. **expert-liaison design thread (vision work, not a fire).** System `expert-liaison` (`recmzKMV2Judg4Sg3`, emerging) gained two v0 artifact sketches, both registered as Assets (Reconciled=false):
   - `practices/revops/ARTIFACT-prospect-discovery-package-2026-05-19.md` — cold-start intake: seed → engine runs speculatively → confidence-labeled discovery package before a prospect session.
   - `practices/revops/ARTIFACT-contact-icp-document-2026-05-19.md` — human-facing control surface Ellie reads/edits to govern who lands on contact lists. Contact-grain; **references but NEVER re-authors the canonical account-grain segment artifact** (memory CRITICAL).
   Registry Roadmap row `recMf1SkC8GO8GFg4` (proposed): the binding loop — deterministic derive doc→{personaResidual, seniority[], department[], minScore} + version-stamp + ICP version on every scored contact. **Non-negotiable design point: a doc Ellie edits is inert unless that derive+version+provenance loop exists.** Both artifacts are sketches pending Nick iteration (open questions in each file). Not built, not bound, no engine change made — Persona Rules table deliberately NOT hand-mutated (that's the build ticket, gated).

8. **Contact-sourcing reopened — completion redefined.** Prior 5-DoD closure measured *data trust*, NOT *delivery of qualified contacts*. MeiraGTx run produced 5 contacts, ALL `icp_filtered_out` (correctly — execs, not CMC buyers) → zero deliverable to Ellie. Root defect: sourcing pulls broad and rejects; it never *targets* the persona, so a good target company can yield nobody. Also: the per-contact ICP reason the scorer generates is NOT persisted on any Contacts field (score lands, why evaporates) — the governance/explainability gap Nick keeps naming. Persona "standard" lives only as Persona Rules config, no human-viewable surface (Contact ICP doc sketch = the answer, unbuilt). **Nick-authorized this session:** ticket `practices/revops/workflows/PROMPT-contact-sourcing-icp-reason-and-3co-run-2026-05-19.md` — (1) add `ICP Score Reason` field + persist per-contact rationale; (2) bounded PAID run on exactly Voyager Therapeutics, Lexeo Therapeutics, Solid Biosciences Inc (domain precondition per company; Hunter-first hard-depends on Target Companies Domain; size:5/co; static selective filter). Diagnostic purpose: does the workflow EVER yield a score≥60 contact with a visible reason. Owner: Workflows. agentic-systems surface-verifies. No spend beyond these 3.

9. **DEFINED PATH TO DONE — engine legibility (not more workflow features).** Nick correctly named that we've been in a defect-fixing loop on the machine while the Companies table stayed strata of half-runs (5 surfaced = 2 hollow / 1 self-contradiction "surfaced+archived_out_of_industry" Adrenas / 2 dubious mega-pharma). Real defect: no legible state column — `surfaced` means nothing in itself; Nick has been forced to infer trust from empty fields. Counts on surface: 47/122 have Last Enriched At, 29/122 have Deep Enrichment Raw — so enrichment HAS run on a subset; "nothing is enriched" is wrong as a blanket but the surfaced set specifically is incoherent. v0 artifact: `practices/revops/ARTIFACT-company-state-model-2026-05-19.md` — Part A: ordered lifecycle column `raw → enriched → classified → icp_validated → expert_ready` + terminal `excluded`, each with observable entry gate + sole writer, state is lower-bound (never claims more than data backs), kills the surfaced-AND-archived contradiction. Ellie's view = `state=expert_ready` only. Part B: per-field provenance map (purpose / writer / mechanism status: working|orphaned|drift|manual-only|under-documented) — many Companies fields self-declare their writer in description; cross-ref against deployed workflow writes yields the orphan count (THE trust-restoring number, answers "I bet many fields can't be populated"). Path to done (3 bounded steps, NO new engine features): audit → reconcile → one clean cohort run. Registry Roadmap row `recG38TbAqKYZ0wNp` (proposed) under revops-engine with concrete Done When. **This is the defined finish line; stop adding machine fixes after the 3-company diagnostic.**

10. **Field provenance audit (Step 1 of state-model path-to-done) — DONE.** `practices/revops/AUDIT-companies-field-provenance-2026-05-19.md`. Method: programmatic cross-ref of Companies schema vs. n8n airtable write-nodes targeting `tblnj3YlOI3thjrXp` (name-filtered 40 candidates of 200 workflows; broader sweep deferred to v2). **Counts: total 135, working 41, computed 1, not_written 93** (~69% of the table has no deployed-workflow writer — Nick's instinct was correct). 9 writer workflows, all inactive, include ~5 likely duplicates (L2 Classify ×3 incl. "dupe", Companies Enrichment ×2, via-agent ×2) — orphaned-workflow spaghetti to retire. Drift refs: `DNC Opt Out` and `Last Contacted Date` (both renamed in schema; workflow writes silently no-op). Caveat: `not_written=93` conflates true orphans with intentional manual-only fields (e.g. `Ellie Note`, `AE Cleared By/At`); v2 audit should separate those before deletion. Next: Nick reviews audit; field-level deletion plan with manual-only carve-outs; relay reducer ticket; workflow spaghetti retirement (separate cleanup).

11. **Reducer fix ticket WRITTEN, awaiting relay.** `practices/revops/workflows/PROMPT-contact-sourcing-collect-prospects-reducer-fix-2026-05-19.md`. Fixes `Collect All Prospects` calling `$('Normalize Prospects').all()` outside the SplitInBatches loop (15→5 silent drop, exec 80833 surface-verified). DoD = bounded paid re-run on Voyager + Solid (the dropped pair), Normalize 2r/10i → Collect 10i → downstream 10 prospects, both companies present on surface. Required precondition before any full-cohort run.

12. **Lifecycle State reconciliation — DONE on all 122 companies.** New singleSelect field `Lifecycle State` (`fldmWpRzOi0bSN34U`) created on Companies; state derived from each row's observable data per the state-model artifact. Distribution: **expert_ready=0, icp_validated=0, classified=1 (BioMarin), enriched=28, raw=82, excluded=11.** Pfizer/Adrenas/MeiraGTx all `excluded` (currency=discontinued or enrichment_status=archived_out_of_industry — MeiraGTx surprise: this session's contact-sourcing runs were on a company already flagged out-of-industry). Lexeo/Voyager/Solid all `raw` (contact-sourcing populates contacts only, not company enrichment). Per-company report: `practices/revops/RECONCILE-companies-lifecycle-state-2026-05-19.md`. Open tuning question: `icp_validated` email gate currently requires {Verified, Catch-all (unconfirmed)}; Lexeo's 2 employer-confirmed CTOs (score 85) have `Unverifiable` emails and so don't qualify — should the gate accept `Unverifiable` when employment is LinkedIn-verified? Single decision that unlocks the most realistic expert_ready candidate.

## Resume point — first actions
1. Read the registry Operating Model table + canon-log. Surface-confirm: Companies=121; L1 186 signals; L2 deployed; contact-sourcing exec status.
2. **Task B `wIyuFELxzXMgHCDV` is CORRUPT — never run it until B's mappings re-verify clean (workstream 3).** A & C are deployed+verified. Corrective ticket out to E-D: `PROMPT-taskb-corruption-fix-2026-05-19.md`. When E-D reports the B fix, surface-verify "Write Company Events" actually targets Company Events `tblnzX2b2kqNGzW6r` with the plan Step 3 field map (Event Type=program_status, Vitality, Is Latest=true, etc.) and the USPTO cred is gone — field-by-field, not validate_workflow. Only after that clean check does the Nick-approved 2-company (Pfizer+Adrenas) paid test run.
3. Still with Nick to relay: contact-sourcing fixes → Workflows; Anthropic credential attach (Workflows lane, independent of currency).
4. When any builder reports back (via Nick): independently surface-verify, do not trust the report. Decisive checks — currency: with a real trade-press `ended` signal in Company Events, do Pfizer/Adrenas route to needs-review not `current`; contact-sourcing: MeiraGTx contacts written with non-zero plausible ICP scores, no upsert errors.
5. Nothing is running, no spend has occurred. Idle-waiting on builders is correct; do not invent work.
